# Build directories
build/
out/
cmake-build-*/

# IDE files
.vs/
.vscode/
.idea/
*.user

# CMake generated
CMakeCache.txt
CMakeFiles/
cmake_install.cmake

# 原 Shin 仓库参考副本（构建不依赖；webview 代码已 vendor 到 src/webview 与 third_party/webview）
third_party/Shin/

# 前端：依赖与 Vite 构建产物（由 pnpm / vite 生成）
# 刻意写成路径无关：ui/ 以后若搬到 apps/<app>/ui/，这里不必再回来改
node_modules/
dist/

# 本地工作区数据（AI 会话记忆、临时日志与截图），不入库
.workbuddy/
